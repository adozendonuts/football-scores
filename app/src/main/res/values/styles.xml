<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <!-- Widget Style-->
    <style name="ScoreBox" parent="Base.Theme.AppCompat.Light">
        <item name="android:paddingLeft">@dimen/widget_padding</item>
        <item name="android:paddingStart">@dimen/widget_padding</item>
        <item name="android:paddingRight">@dimen/widget_padding</item>
        <item name="android:paddingEnd">@dimen/widget_padding</item>
    </style>

</resources>
